<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked + Pressed -->
    <item android:color="?attr/colorSecondary" android:state_pressed="true" android:state_checked="true"/>
    <!-- Unchecked + Pressed -->
    <item android:color="?attr/colorSurfaceVariant" android:state_pressed="true" android:state_checked="false"/>
    <!-- Checked -->
    <item android:color="?attr/colorPrimaryContainer" android:state_checked="true"/>
    <!-- Unchecked -->
    <item android:color="?attr/colorSurface"/>
</selector>